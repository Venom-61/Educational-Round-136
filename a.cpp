#include<bits/stdc++.h>
using namespace std;

#define ll long long

// A. Immobile Knight

void solve() {
    int n, m;
    cin >> n >> m;
    cout << n/2 + 1 << " " << m/2 + 1 << "\n";
}

int main() {
    ios_base::sync_with_stdio(false);
    cin.tie(nullptr);

    int test = 1;
    cin >> test;
    while(test--) {
        solve();
    }

    return 0;
}
